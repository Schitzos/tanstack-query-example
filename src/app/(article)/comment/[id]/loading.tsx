import LoadingSpinner from '@/components/elements/loadingSpinner';
import React from 'react';

export default function LoadingArticle() {
  return (
    <LoadingSpinner text='Loading from server'/>
  );
}
